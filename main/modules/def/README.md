# 🛸 VSCode Backup Automation

> Автоматизированная система бэкапа настроек VS Code прямо в Telegram.

---

## 📑 Оглавление

* [📌 Требования](#-требования)
* [⚙ Настройка системы](#-настройка-системы)
  * [1. Подготовка GitHub](#этап-1-подготовка-github)
  * [2. Настройка локального ПК (Git)](#этап-2-настройка-локального-пк-git)
  * [3. Настройка Python-скрипта](#этап-3-настройка-python-скрипта)
  * [4. Установка модуля в Heroku](#этап-4-установка-модуля-в-heroku)
* [🕒 Автоматизация запуска](#-автоматизация-запуска)
  * [1. Windows (Планировщик задач)](#1-windows-планировщик-задач)
  * [2. Linux (Cron)](#2--linux-cron)

* [🆘 Решение проблем](#-решение-проблем)
* [🛠 Команды модуля](#-команды-модуля)


---

## 📌 Требования

* **ПК:** Установленный [Git](https://git-scm.com/downloads) и [Python 3.8+](https://www.python.org/).
* **Heroku:** Установленный юзербот на любом хостинге.
* **GitHub:** С доступом к созданию приватных репозиториев.


---

## ⚙ Настройка системы

## Этап 1: Подготовка GitHub

### 1. Создание репозитория

1. Зайдите на [github.com/new](https://github.com/new).
2. **Repository name:** Например, `vscode-backup`.
3. **Public/Private:** Обязательно выберите **Private** (чтобы ваши токены и настройки в VS Code не стали публичными).
4. Нажмите **Create repository**.

### 2. Получение токена (PAT Classic)

Чтобы модуль Hikka мог залезть в ваш приватный репозиторий, ему нужен «паспорт».

1. Перейдите в [Settings -> Developer Settings -> Personal access tokens -> Tokens (classic)](https://github.com/settings/tokens).
2. Нажмите **Generate new token (classic)**.
3. В поле **Note** напишите `Heroku-Backup`.
4. В разделе **Select scopes** отметьте галочку **`repo`** (это даст доступ к чтению и скачиванию файлов).
5. Нажмите **Generate token**.
6. **ВАЖНО:** Скопируйте токен сразу. Он больше никогда не будет показан.

---

## Этап 2: Настройка локального ПК (Git)

Чтобы скрипт мог отправлять файлы, ваш компьютер должен быть авторизован в GitHub. Самый простой способ — использовать **Git Credential Manager**.

### 1. Проверка путей VS Code

Вам нужно знать, что именно бэкапить. Основные папки настроек (`User`):

* **Windows:** `%APPDATA%\Code\User`
* **Linux:** `~/.config/Code/User`
* **macOS:** `~/Library/Application Support/Code/User`

### 2. Подготовка локальной директории

Выберите место на диске, где будет лежать локальная копия вашего бэкапа (например, `D:\backups\vscode_git`).

1. Откройте терминал (CMD или PowerShell).
2. Перейдите в папку: `cd D:\backups\vscode_git`.
3. Инициализируйте репозиторий:
```bash
git init
git remote add origin https://github.com/ВАШ_НИК/vscode-backup.git
```


4. Сделайте любой тестовый файл, чтобы создать ветку:
```bash
echo "init" > init.txt
git add .
git commit -m "initial commit"
git branch -M main
git push -u origin main
```


*На этом этапе всплывет окно авторизации GitHub. Войдите в аккаунт.*

---

## Этап 3: Настройка Python-скрипта

Файл `pc-logic.py` должен лежать на ПК.

### Как найти правильный путь в Python?

В скрипте используется библиотека `os`. Чтобы убедиться, что путь верный, проверьте:

* Для Windows: `os.path.join(os.getenv('APPDATA'), 'Code', 'User')` — это стандарт.
* Если у вас **VS Code Insiders**, путь будет `.../Code - Insiders/User`.

**Логика работы скрипта:**

1. Он берет всё содержимое из папки настроек.
2. Упаковывает в `vscode_backup.zip`.
3. Копирует этот ZIP в папку репозитория.
4. Выполняет команды `git add`, `git commit` и `git push`.

---

## Этап 4: Установка модуля в Heroku

1. Скопируйте код модуля `VSCodeBackup.py`.
2. В Telegram отправьте файл самому себе/в любой чат и напишите в ответ на него `.dlm`.
3. Перейдите в настройки: `.config VSCodeBackup`.

### Заполнение полей:

* **`repo`**: В формате `пользователь/репозиторий` (например, `ivanov/vscode-backup`). **БЕЗ** полной ссылки https.
* **`github_token`**: Тот самый токен, который мы создали на Этапе 1.
* **`backup_chat_id`**: ID чата.
  * *Как узнать?* Зайдите в нужный чат и напишите `.id`.
> **Важно:** Если это группа, ты должен быть там администратором.

---

## 🕒 Автоматизация запуска

### 1. Windows (Планировщик задач)

---

Чтобы бэкап происходил сам по себе без твоего участия, нужно настроить **Планировщик задач Windows**. Скрипт будет запускаться в фоновом режиме, архивировать данные и отправлять их на GitHub по расписанию.

### Инструкция по настройке:

1. **Открой Планировщик:**
* Нажми `Win + R`, введи `taskschd.msc` и нажми Enter.


2. **Создай задачу:**
* В правой панели выбери **"Создать простую задачу..."** (Create Basic Task).
* **Имя:** `VSCode_Git_Backup`.
* **Триггер:** Выбери "Ежедневно" (или "При входе в систему"), укажи удобное время (например, 03:00, когда работа завершена).


3. **Действие:**
* Выбери **"Запустить программу"** (Start a program).
* В поле **"Программа или сценарий"** введи: `pythonw.exe`.
> **Важно:** Использование `pythonw.exe` вместо `python.exe` позволит скрипту запускаться без всплывающего черного окна консоли.


* В поле **"Добавить аргументы"** впиши полный путь к твоему скрипту, например: `C:\Scripts\backup.py`.
* В поле **"Рабочая папка"** укажи путь к папке, где лежит скрипт: `C:\Scripts\`.


4. **Условия и параметры:**
* В окне завершения нажми "Готово".
* Найди задачу в списке, кликни правой кнопкой -> **Свойства**.
* На вкладке **"Условия"** сними галочку "Запускать только при питании от сети", если используешь ноутбук.
* На вкладке **"Параметры"** поставь галочку "Немедленно запускать задачу, если пропущен плановый запуск".


---

## 2. 🐧 Linux (Cron)

Если ты используешь Linux, используй стандартный планировщик `cron`.

1. Открой редактор крон-таблицы:
```bash
crontab -e
```


2. Добавь строку в конец файла для ежедневного запуска в 3 часа ночи:
```Bash
00 03 * * * /usr/bin/python3 /home/user/scripts/backup.py

```


3. Сохрани и выходи. Теперь система сама будет дергать скрипт.

---

### 💡 Лайфхак для проверки

Чтобы не ждать ночи, после настройки в Планировщике Windows нажми на задачу правой кнопкой мыши и выбери **"Выполнить"**. Если в твоем репозитории на GitHub появился новый коммит — успех.

---

## 🆘 Решение проблем

### 1. Ошибка "Access Denied" или 401 в Heroku

* **Причина:** Неверный токен или у токена нет прав `repo`.
* **Решение:** Перевыпустите токен Classic и убедитесь, что галочка возле `repo` присутствует.

### 2. Ошибка "git is not recognized" на ПК

* **Причина:** Git не добавлен в системную переменную PATH.
* **Решение:** Переустановите Git, отметив галочку "Add to PATH", либо добавьте путь к `bin` вручную в настройках системы.

### 3. Скрипт на ПК "зависает" при пуше

* **Причина:** Git ждет ввода логина/пароля в фоновом режиме.
* **Решение:** Попробуйте сделать `git push` вручную в консоли один раз. Если он спросит пароль — введите его. После этого он сохранится в системе.

### 4. Конфликты процессов

Скрипт архивации может выдать ошибку, если VS Code открыт и какой-то файл (например, база данных `globalStorage`) заблокирован.

* **Решение:** Скрипт в коде использует стандартное копирование. Если ошибки будут повторяться, рекомендуется закрывать VS Code перед бэкапом.


---

## 🛠 Команды модуля

* `.getbackup` — принудительно вытянуть последний архив из GitHub и прислать в чат.

---
<div align="center">

![Banner](./assets/sss.png)
</div>